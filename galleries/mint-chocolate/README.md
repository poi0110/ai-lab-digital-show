# 薄荷巧克力个人IP数字展厅

直接双击 `index.html` 即可浏览。若浏览器限制本地视频自动播放，可点击 Banner 中的“点击播放角色视频”按钮。

也可以在本目录运行：

```bash
node server.mjs --port 4173
```

然后在浏览器中访问 `http://localhost:4173/`。

网页不调用外部图片、视频、字体或 API；角色视频已转为 H.264 MP4（AAC 音频、fast start）。
